This project aims to parse software engineering textbooks to create a RAG (Retrieval Augmented Generation) Pipeline for code + text embeddings.

To install the dependencies listed in the requirements.txt file, run the following command (preferably in a virtual environment):

`pip install -r requirements.txt`
